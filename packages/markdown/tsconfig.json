{
  "extends": "@doska/configs/tsconfig/base.json",
  "compilerOptions": {
    // No DOM: this package must stay renderable on any platform, so anything
    // reaching for `document` or an HTML element type fails to compile.
    "lib": ["ES2023"],
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
